<Window x:Class="SEW.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SEW"
        mc:Ignorable="d"
        Title="SEW - Study English Words" Height="500" Width="900" ResizeMode="NoResize" Background="#242424">
    <DockPanel>
        <Border DockPanel.Dock="Left" Background="#202020" Width="150">
            <StackPanel>
                <Button Content="   Учить" Style="{DynamicResource SLeftPanel}" Template="{DynamicResource TLeftPanel}"/>
                <Button Content="   Категории" Style="{DynamicResource SLeftPanel}" Template="{DynamicResource TLeftPanel}"/>
                <Button Content="   Статистика" Style="{DynamicResource SLeftPanel}" Template="{DynamicResource TLeftPanel}"/>
                <Button Content="   Настройки" Style="{DynamicResource SLeftPanel}" Template="{DynamicResource TLeftPanel}"/>
            </StackPanel>
        </Border>
        <Grid>
            <local:Categories></local:Categories>
        </Grid>
    </DockPanel>
</Window>
